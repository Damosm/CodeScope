# Moteur d'analyse

Le scanner exclut `.git`, `.vs`, `bin`, `obj`, `node_modules` et `packages`. XML fournit frameworks et références ; Roslyn extrait types et méthodes. La complexité vaut 1 plus les branches, boucles, cas, expressions conditionnelles et opérateurs logiques. Les lignes correspondent à la portée syntaxique inclusive.
